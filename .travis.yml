language: csharp
script:
  - xbuild /p:Configuration=Debug Test.sln
before_install:
  - sudo apt-get install nunit-console
before_script:
  - nuget restore Test.sln
after_script:
  - nunit-console Editor/Test/bin/Debug/Test.dll
